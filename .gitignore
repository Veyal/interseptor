# OS / editor cruft
.DS_Store
*.swp

# Personal, per-user Claude Code overrides
.claude/settings.local.json

# Compiled binary
/interceptor

# Local runtime data (created under ~/.interceptor by default; ignore if run in-tree)
/.interceptor/
